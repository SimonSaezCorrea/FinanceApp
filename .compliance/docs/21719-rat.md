# Registro de Actividades de Tratamiento (RAT)

**Empresa:** FinanceApp (proyecto/marca temporal — sin constitución societaria formal; opera como persona natural) · **RUT:** [COMPLETAR: RUT personal de Simón Sáez — no se proporcionó en esta corrida] · **Domicilio:** La Florida, Santiago, Chile (comuna declarada; sin dirección formal de oficina)
**Responsable de datos:** Simón Sáez (founder / responsable designado) · **Contacto:** simon.alejandro.saez@gmail.com
**Fecha:** 2026-09-19 · **Versión:** 1.0 (primera corrida de `compliance-cl`, commit `81430dc`)

> Documento interno exigible por la Agencia de Protección de Datos. Mantener actualizado. Generado auditando el repositorio `FinanceApp` (monorepo pnpm/Turborepo, `apps/api` NestJS + Prisma/PostgreSQL, `apps/web` Vite/React).

| # | Actividad / finalidad | Rol | Categorías de datos | Categorías de titulares | Base de licitud | Sub-encargados / proveedores | Transferencia internacional | Retención | Medidas de seguridad |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Gestión de cuenta de usuario (registro, login, perfil) — `apps/api/src/domains/user` | Responsable | Nombre, email, teléfono, RUT/DNI/pasaporte, dirección, fecha de nacimiento, contraseña (solo hash) | Usuarios registrados | Ejecución de contrato (Art. 12); dato de identidad para poder prestar el servicio | Ninguno externo (autenticación propia, sin proveedor de identidad de terceros) | No | Mientras la cuenta esté activa. **Sin plazo definido tras baja** — usuario no tenía política de retención definida en el cuestionario; se propone default en la Política de Privacidad (sección 5) | Contraseñas con `bcryptjs` (`register.handler.ts:3`), JWT en cookies httpOnly, MFA TOTP opcional cifrado AES-256-GCM (`mfa-secret-cipher.ts`), passkeys/WebAuthn opcional, `JwtAuthGuard` revoca acceso de inmediato al cerrar sesión |
| 2 | Gestión financiera personal (cuentas bancarias, tarjetas, movimientos, deudas, ahorros, cuotas, facturación de crédito) — `apps/api/src/domains/{bank-account,transaction,card-account,debt,savings-goal,installment-plan,credit-statement}` | Responsable | Saldos, movimientos de ingreso/gasto, categorías, últimos 4 dígitos de tarjeta, deudas, metas de ahorro, cupos de crédito | Usuarios registrados | **Dato sensible por "situación socioeconómica" (Art. 2 letra g)** — requiere consentimiento **expreso y reforzado** (Art. 16). **❌ No implementado hoy**: no existe checkbox de consentimiento en el registro (`apps/web/src/domains/auth`) | Ninguno externo — el dato financiero nunca sale de la infraestructura propia | No | Sin plazo definido — mismo hueco que la fila 1, agravado por ser dato sensible (Art. 16 exige minimización estricta) | Aislamiento estricto por `userId` en 29 de 40 repositorios Prisma (los 11 restantes son tablas de catálogo global sin dueño: país, moneda, institución financiera — correcto por diseño); identificadores UUID v7 no adivinables con validación de formato en el borde (specs/016); cursor de paginación firmado con HMAC (specs/017) |
| 3 | Comprobantes de movimientos (adjuntos: boletas/vouchers) — `apps/api/src/domains/transaction-attachment` | Responsable | Archivo adjunto — puede contener datos personales/financieros propios o de terceros (ej. nombre de un tercero en una boleta) | Usuarios registrados (y posibles terceros mencionados en el comprobante) | Ejecución de contrato / interés del usuario en registrar su gasto | Almacenamiento S3-compatible, proveedor a elección (AWS S3 / MinIO / R2 / Backblaze vía `S3_ENDPOINT`) — **hoy INERTE**: sin `S3_BUCKET`/credenciales configuradas, el endpoint responde `503 ATTACHMENTS_UNAVAILABLE` | **Depende del proveedor que se active** — si se configura AWS S3 en una región fuera de Chile, sí hay transferencia y requiere mecanismo (ver `21719-anexo-transferencias.md`) | Mientras exista el movimiento asociado | URL de descarga firmada con vigencia de 5 minutos; `storageKey` opaca (UUID v4, sin relación con `userId`/nombre de archivo, specs/017) |
| 4 | Seguridad de sesión (dispositivo, IP, geolocalización aproximada) — `apps/api/src/domains/{session,ip-geolocation-cache}` | Responsable | IP de origen, tipo de dispositivo/navegador (derivado del User-Agent), país y ciudad aproximados | Usuarios registrados | Interés legítimo de seguridad (detectar accesos desde dispositivos nuevos) | **IPinfo.io** (`api.ipinfo.io`, servicio extranjero) cuando `IPINFO_TOKEN` está configurado — **transferencia internacional ACTIVA de la IP de login**; alternativa local sin transferencia: MaxMind GeoLite2 vía `GEOIP_DB_PATH` (archivo local, sin llamada de red) | **Sí, si `IPINFO_TOKEN` está configurado** — ver `21719-anexo-transferencias.md` | Sesión: hasta el cierre + 3 días de retención, purgada por cron diario. Caché de geolocalización por IP: 60 días, purgada por cron diario | `ua-parser-js` (parseo local, sin red), caché interna evita repetir la consulta a IPinfo para la misma IP |
| 5 | Autenticación multifactor (TOTP y llaves de acceso/Passkeys) — `apps/api/src/domains/{mfa-recovery-code,passkey}` | Responsable | Secreto TOTP (cifrado), códigos de recuperación (hash), clave pública WebAuthn | Usuarios que activan MFA/passkey (opcional) | Consentimiento del usuario (función de seguridad que el usuario activa voluntariamente) | Ninguno externo | No | Mientras el mecanismo esté activo | Secreto TOTP cifrado AES-256-GCM (`MFA_ENCRYPTION_KEY`), códigos de recuperación con `bcryptjs`, clave privada de passkey nunca sale del dispositivo del usuario (estándar WebAuthn) |

## Notas
- Distinguir cada flujo: en este proyecto **todos los flujos son "responsable"** — FinanceApp no procesa datos por cuenta de un tercero (no hay clientes B2B), así que no aplica el rol "encargado" hoy.
- **Hallazgo priorizado**: la fila 2 (dato financiero = dato sensible por "situación socioeconómica") no tiene hoy el consentimiento expreso y reforzado que exige el Art. 16. Ver `21719-consentimiento.md` y la EIPD (`21719-eipd.md`).
- Revisar al menos cada 12 meses o ante cambios de infraestructura (ej. si se activa el bucket S3 o se agrega un proveedor de correo).

---
*Borrador generado con compliance-cl (pack ley-21719). No constituye asesoría legal; revisar con un abogado.*
